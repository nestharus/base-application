# User Service Configuration
server.port=8080
server.host=0.0.0.0

# Metrics
metrics.enabled=true
metrics.port=9090

# Health checks
health.live.path=/health/live
health.ready.path=/health/ready

# Service discovery
service.name=user-service
service.namespace=user-service